.role-indicator{ style: "background: #{@role.color}" }
%span.name!= @link_to @user.name, Routes.user_path(@user.id)
%span.icon
%span.remove &times;
- if @show_time
  .time
    Do
    %time= moment(@to).format('LL')
